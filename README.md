# 음성 녹음기 & 중국어 회화 연습

SvelteKit 기반의 음성 녹음기 및 OpenAI Realtime API를 활용한 실시간 중국어 회화 연습 애플리케이션입니다.

## 기능

- 🎤 **음성 녹음**: 브라우저에서 직접 음성을 녹음하고 재생
- 📊 **실시간 파형 시각화**: 녹음 중 오디오 파형을 실시간으로 표시
- 🗣️ **실시간 중국어 회화**: OpenAI Realtime API를 활용한 실시간 음성 대화
- 💾 **녹음 파일 저장**: 녹음된 파일을 다운로드

## 시작하기

### 1. 의존성 설치

```sh
npm install
```

**중요**: `@openai/agents` 패키지가 설치되지 않은 경우, 다음 명령어로 설치하세요:

```sh
npm install @openai/agents
```

### 2. 환경 변수 설정

프로젝트 루트에 `.env` 파일을 생성하고 OpenAI API 키를 설정하세요.

**📖 자세한 가이드**: [ENV_SETUP.md](./ENV_SETUP.md) 파일을 참고하세요.

**빠른 설정:**

1. **OpenAI API 키 발급**
   - https://platform.openai.com/api-keys 접속
   - "Create new secret key" 클릭
   - 생성된 키 복사 (한 번만 표시됨!)

2. **.env 파일 생성**
   - 프로젝트 루트에 `.env` 파일 생성
   - 다음 내용 입력:
   ```env
   OPENAI_API_KEY=sk-proj-여기에_실제_API_키_붙여넣기
   ```
   - 예시: `OPENAI_API_KEY=sk-proj-abc123def456ghi789...`

3. **서버 재시작**
   ```bash
   npm run dev
   ```

### 3. 개발 서버 실행

```sh
npm run dev

# 또는 브라우저에서 자동으로 열기
npm run dev -- --open
```

## 사용 방법

### 녹음 모드
1. "녹음 모드" 탭 선택
2. "녹음 시작" 버튼 클릭
3. 마이크에 말하기
4. "녹음 중지" 버튼으로 녹음 종료
5. 녹음된 파일 재생 및 저장

### 실시간 대화 모드
1. "실시간 대화" 탭 선택
2. "대화 시작" 버튼 클릭
3. 마이크에 중국어로 말하기
4. AI가 실시간으로 응답
5. "대화 종료" 버튼으로 종료

## 기술 스택

- **SvelteKit**: 프레임워크
- **Tailwind CSS**: 스타일링
- **OpenAI Realtime API**: 실시간 음성 대화
- **Web Audio API**: 오디오 처리
- **Canvas API**: 파형 시각화

## API 엔드포인트

### POST `/api/realtime/key`
Realtime API 세션을 위한 ephemeral key를 생성합니다.

## 빌드

프로덕션 빌드를 생성하려면:

```sh
npm run build
```

프로덕션 빌드를 미리보려면:

```sh
npm run preview
```

> To deploy your app, you may need to install an [adapter](https://svelte.dev/docs/kit/adapters) for your target environment.
